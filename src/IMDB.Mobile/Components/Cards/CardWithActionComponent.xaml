<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="IMDB.Mobile.Components.Cards.CardWithActionComponent"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Name="cardWithAction">
    <Grid
        BindingContext="{Binding Source={x:Reference cardWithAction}}"
        HeightRequest="260"
        RowSpacing="10"
        WidthRequest="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border
            Grid.Row="0"
            Background="{StaticResource ColorTertiary}"
            HeightRequest="150"
            WidthRequest="100">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
        </Border>
        <ffimageloading:CachedImage
            Grid.Row="0"
            Aspect="AspectFill"
            CacheDuration="30"
            DownsampleToViewSize="True"
            ErrorPlaceholder="erro_icon_thumb_not_load.png"
            Source="{Binding ImageSource}" />
        <Label
            Grid.Row="1"
            FontFamily="PoppinsRegular"
            FontSize="12"
            HorizontalOptions="Center"
            Text="{Binding Title}"
            TextColor="White" />
        <Image Grid.Row="2" Source="trash.png">
            <Image.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding DeleteCommand}" CommandParameter="{Binding Id}" />
            </Image.GestureRecognizers>
            <Image.Behaviors>
                <toolkit:IconTintColorBehavior TintColor="{StaticResource ColorSecondary}" />
            </Image.Behaviors>
        </Image>
    </Grid>
</ContentView>
