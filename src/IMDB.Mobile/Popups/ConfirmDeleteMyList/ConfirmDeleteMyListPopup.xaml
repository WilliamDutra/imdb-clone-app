<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup
    x:Class="IMDB.Mobile.Popups.ConfirmDeleteMyList.ConfirmDeleteMyListPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:language="clr-namespace:IMDB.Mobile.Resources"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Name="confirmDeleteMyListPopup"
    Background="{StaticResource ColorPrimary}"
    HeightRequest="200"
    WidthRequest="250">
    <VerticalStackLayout Spacing="16">
        <Label
            FontFamily="PoppinsBold"
            FontSize="20"
            HorizontalOptions="Center"
            Text="{x:Static language:Resource.label_confirm_delete_list_popup}"
            TextColor="White"
            VerticalOptions="Center" />
        <Button
            Background="{StaticResource ColorSecondary}"
            Command="{Binding ConfirmCommand}"
            Text="Sim" />
        <Button
            Background="Red"
            Command="{Binding CancelCommand}"
            Text="Não" />
    </VerticalStackLayout>
</toolkit:Popup>